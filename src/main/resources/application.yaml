spring:
  datasource:
    url: jdbc:mysql://localhost:3306/real_estate_db?createDatabaseIfNotExist=true
    username: root
    password: 2424
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true